# AGENT.md

## 最高优先级

- 本文件是项目长期记忆，路径为根目录 `AGENT.md`。
- 每次开始处理用户的新任务前，都必须先从头完整重读根目录 `AGENT.md`，再进行检索、分析、编码或回答；不得因当前上下文中曾经读过而跳过。
- 每次上下文压缩、摘要、模型切换、任务恢复或长期中断后，必须再次从头完整重读本文件；不得只依赖摘要或历史记忆。
- 用户说“看代码”“读取代码”“搜索代码”“结合代码分析”等且未另行指定目录时，默认指 `F:\ProductionSource` 下当前生产环境使用的前后端源码，不是 `F:\PMproject` 的原型代码。只有用户明确说“原型代码”“当前项目代码”或指定 `F:\PMproject` 时，才检索本原型项目。
- 所有对话、进度、文档和原型业务文案默认使用简体中文。
- 用户最新明确要求高于本文件。若两者冲突，先指出影响，再执行用户决定。
- 先读取需求原文、修改文档和现有实现；发现会改变业务结果的歧义时聚焦询问，不自行补业务定义。
- `docs/` 只保存本地需求与规范，必须被 `.gitignore` 排除，禁止提交或推送。

### 已完成需求逻辑锁（不可越过）

- `data.js` 中状态为【已完成】的需求在本地视为只读锁定；没有用户针对该需求的明确修改指令时，禁止修改其数据、页面、说明、交互、样式、路由或任何会改变最终展示结果的共享逻辑。
- “顺便优化”“统一处理”“公共组件调整”或当前实现更合理，都不能作为跨越逻辑锁的理由。公共修改可能影响已完成需求时，必须先隔离影响；无法隔离时必须停下并向用户确认。
- 只有用户主动明确要求修改某个已完成需求，或明确要求“所有需求”执行某项修改时，才允许解锁本次指令覆盖的最小范围；一次授权不得扩展到其他需求、其他页面或后续修改。
- 修改已完成需求前，必须先在本地 `docs/GitHub推送待确认事项.md` 的【待确认】中登记：需求编号、用户授权原文、计划修改内容和登记时间；完成后补充实际修改与验证结果。未登记不得开始编辑。
- `docs/GitHub推送待确认事项.md` 必须始终由 `.gitignore` 排除，不得暂存、提交或推送；它是本地发布闸门，不是线上需求文档。
- GitHub 发布前必须读取该台账。存在尚未发布的已完成需求改动时，必须逐条向用户说明“哪个已完成需求、改了什么”，并取得针对当前清单的明确上传确认；确认前禁止暂存、提交和推送。
- 用户确认只对当时展示的改动清单有效。确认后若又修改了任何已完成需求，必须重新登记并再次确认；不得沿用旧确认。
- 用户拒绝或暂未确认上传时，改动只保留本地，台账保持【待确认】状态。推送成功后才可将条目移至【已推送记录】，补充提交号和推送时间。
- 上述逻辑锁优先于开发便利、上下文摘要、自动压缩、历史习惯和普通发布指令。除非用户主动明确授权对应修改，否则绝不能跨越。

## 项目基线

- 这是供产品、开发、测试评审的在线高保真原型库，不是最终 UI 视觉稿，也不承载生产业务。
- 默认使用原生 HTML/CSS/JavaScript 和静态数据，可直接静态部署；没有多人编辑、权限、审计或在线保存需求时不引入数据库。
- 本项目没有后端服务；本地预览统一在项目根目录启动静态 HTTP 服务，固定访问地址为 `http://127.0.0.1:4173/`。用户可双击根目录 `start-prototype-server.bat` 启动，不需要每次通过 Codex 启动。不得改用 `8000` 或其他端口，除非 `4173` 被占用或用户明确指定；端口被占用时先检查是否已是本项目服务。
- 需求与说明数据集中在 `data.js`，交互和渲染在 `app.js`，视觉事实来源是 `styles.css`。
- 产品负责人默认 `Mike`，页面环境标识统一为“产品原型”。
- 需求编号可能包含 `#` 等字符，写入 hash 路由时必须 `encodeURIComponent`，读取时必须解码。
- 每次新增或修改需求、页面、交互、说明或公共规范，都要同步该需求的 `updatedAt`；列表“最后更新”从数据计算，禁止硬编码。
- 修改前检查工作区，保留用户已有改动；未经明确要求不提交、不推送、不执行破坏性 Git 操作。

## 固定信息架构

- 第一层是需求列表，至少展示：需求编号、主题与副标题、优先级、状态、产品、开始时间、完成时间、最后更新时间和查看入口。
- 点击需求进入详情；深链接刷新后必须恢复同一需求和页面。
- 详情页固定为左侧高保真原型约 `80%`、右侧说明区约 `20%`。
- 返回需求列表按钮固定在右侧说明区最顶部。
- 一个需求有多个页面时，导航放在左侧原型内部，不新增第三栏。
- 后台端本身只作为产品/端标识，不得伪装成菜单节点。后台信息架构统一按“一级菜单 → 二级菜单 → 三级页面Tab”表达：一级菜单及其二级菜单都放在左侧菜单栏，并明确显示父子层级；只有同一二级菜单内部的多个页面视图才使用内容区三级Tab。
- 同一二级菜单下的三级菜单统一在左侧原型内容区使用页内 Tab 展示；侧边菜单只保留二级菜单入口，不得把三级菜单继续缩进堆叠在侧边栏。
- 窄屏可改为上下结构，但原型、返回入口和说明关联均不能丢失。

## 原型与说明关联

- 有独立业务规则的组件必须分配稳定 ID，并在左侧组件和右侧说明同时展示。
- ID 前缀：`F` 字段、`B` 按钮/命令/链接、`T` 表格、`C` 重复项、`M` 弹窗/抽屉、`P` 面板、`S` 状态、`N` 导航；格式为前缀加两位数字。
- 页面内 ID 唯一；完整引用为 `需求编号/页面编号/组件ID`。已评审 ID 不随意复用或改义。
- 点击左侧组件时，右侧对应说明滚动到视区中央并高亮；点击或键盘激活右侧说明时，左侧组件定位并高亮。
- 悬停关联只能作为辅助，不能替代点击和键盘操作。
- 组件角标不得改变布局尺寸，也不得被禁用态降低可读性。
- 左右角标完全一致：背景 `#B70000`、文字 `#FFFFFF`。左侧选中描边用 `#d19a36`，右侧选中卡片用 `#186b4d`。
- 右侧说明只记录开发无法从界面直接判断的业务规则、数据来源、状态转换、权限、异常和边界；不重复表头、按钮名称、AND 关系等常识。
- 完全新增的页面默认展示简短的“页面目标”，只描述最终能力；生产现有页面的字段调整、内容优化、合并、拆分或迁移默认不展示页面目标，除非用户明确要求保留。新增属性必须通过页面配置显式标记，不得仅根据存在 `purpose` 自动判断。
- 主流程默认不展示；只有用户明确要求某个页面保留时，才通过页面配置显式展示，不得根据已有 `flow` 数据自动恢复。
- 技术说明只描述最终要实现的状态和规则，不叙述需求形成、开发阶段、曾经增加后又删除等过程；历史过程只有在直接影响兼容、迁移或数据处理时才可保留。
- 待确认事项集中展示；不得把未确认内容写成确定结论。问题解决后删除对应待确认项。

## 生产页面差异聚焦

- 基于生产页面仅做局部修改时，必须保留生产结构作为上下文，但未修改区域统一降为约 `22%` 不透明度并灰度处理；禁止使用模糊或直接置空，以免丢失与生产页面的对应关系。
- “低对比度的是和生产一致，无需修改”等对比口径、记录范围、数据范围、评审提示和开发说明只放在右侧说明区顶部，禁止放进左侧原型业务画布；否则开发可能误认为它们是生产页面元素。左侧只展示最终需要实现的生产界面、交互控件和必要的原型说明角标。
- 本次变更组件保持正常对比度并保留红底白字说明角标；右侧说明优先只列本次变化，但用户明确要求保留的跨端规则、业务规则不得删除。
- 生产现有模块仅修改标题或字段名称、统计口径和交互均不变时，只让发生变化的标题或字段名保持正常对比度；模块结构、数值、辅助信息和未变化控件继续弱化。禁止把整张卡片或整个模块误标为新增修改。
- 未修改控件在原型中默认不可操作；若它是打开本次变更弹窗或抽屉的必要入口，则该入口单独恢复正常对比度和交互，其余同区域内容继续弱化。
- 弹窗只有局部字段变化时，弹窗其他区域同样弱化，只突出发生变化的字段组；不得让整个弹窗看起来都属于本次修改。
- 生产数据表仅调整字段时，只高亮发生变化的表头；表格正文统一弱化，不因所属列发生变化而高亮。只有正文内容本身属于新增展示时，才允许单独恢复正常对比度。
- 完全新增的页面不显示“未标注的地方均为未修改”等调整说明，必须展示明确、面向最终实现的页面目标；新增页面所有真实功能均按正常对比度展示。

## 公共组件目录

后续需求必须先复用下列能力，再考虑新增近似实现。当前是原生函数和样式，未来迁移框架时保持相同接口与视觉结果。

- `RequirementList`：需求列表、状态、更新时间、可访问行点击和编号路由编码。
- `PrototypeDetailShell`：`80/20` 双栏、原型上下文栏、业务侧栏、右侧说明区和返回入口。
- `PageNavigation` / `InnerTabs`：需求页面导航及页面内 Tab，负责选中态与内容切换。
- `MenuChangeBadge` / `MenuMigrationSummary`：在菜单旁标记“合并 N / 新增”，并展示原菜单到新菜单的来源映射；标记必须明确注明只用于原型评审。
- `AnnotationCard` + `ComponentBadge` + `bindComponentLinks`：组件 ID、左右双向定位、悬停和键盘激活。
- `TabScopedAnnotations`：同一二级菜单包含多个 Tab 时，右侧只展示当前 Tab 的组件说明；菜单级说明始终保留，切换 Tab 后同步更新说明数量和双向绑定。
- `FilterPanel` / `Field` / `FilterActions`：统一筛选网格、输入、下拉、筛选、重置、导出布局。
- `DateTimeRangePicker`：同一弹层内选择开始与结束、双日历、时分秒、快捷范围、边界定位。
- `AutocompleteField`：聚焦展示全部选项、输入过滤、选择回填、失焦收起与 trim。
- `DataTable` + `TableScrollContainer`：表头、复合单元格、固定列、横纵滚动、十行高度限制。
- `Pagination` + `TopPagination`：总数、每页数量、页码、跳页；默认 200 条时顶部与底部联动展示。
- `Modal`：标题、关闭、正文、取消/确认、危险确认、表单和表格内容。
- `RelationListModal`：关联会员、关联 IP、关联设备等“查询条件摘要 + 数据表 + 分页”弹窗。
- `StateTag` / `RiskTag` / `ResultTag`：分类、成功、待处理、拒绝等低饱和标签。
- `StackCell` / `DateTimeCell` / `MoneyCell` / `AgentCell`：表格复合字段的标题和值层级。
- `MemberDetailLink`：会员账号统一可点击，并映射到对应说明；真实目标存在时以 `_blank` 打开会员详情。
- `BatchAction`：复选框、全选、选中数量、无选择禁用和二次确认。
- `VipSiteConfigPanel`：会员等级、红利与VIP返水共用的站点配置选择、默认继承、整套单独配置和恢复默认；同一站点只能整套使用总控默认配置或整套使用本站点独立配置，禁止页面各自维护不同状态或部分继承。
- `EmptyState` / `LoadingState` / `ErrorState`：列表非默认状态，不使用空白表格代替。
- `PrototypeStateToggle`：只用于原型评审切换状态，必须在说明中标明不属于生产功能。
- `ReviewOverlay`：功能待确认时覆盖业务区，展示原因和查看入口；不得覆盖右侧说明区。

公共能力若在第二个页面再次出现，应优先抽取或扩展现有函数/样式，禁止复制出另一套近似组件。抽取不能改变已确认的页面视觉和交互。

菜单变更需要展示评审标记时统一使用以下语义：`合并 N` 表示 N 个原菜单合并为当前菜单，必须列出来源；`新增` 表示无原菜单来源的纯新功能；无修改菜单不显示徽标。用户已明确属于已知修改或不需要标注时，不显示合并、新增等评审徽标。徽标使用低饱和色和明确文字，不能只靠颜色表达。

## 表单与筛选规则

- 原型筛选区每行最多放置 3 个普通筛选项。时间范围筛选框采用 `300-330px` 的紧凑宽度并只占 1 个网格列；开始时间和结束时间必须在同一行完整显示，中间显示【至】，与同排相邻筛选项保持正常网格间距，不得预留额外空列。双日历弹层可独立保持适合操作的宽度。此条仅为原型开发标准，禁止写入右侧开发说明、交付文档或生产页面文案。
- 字段命名统一遵循根目录本地文档 `docs/统一字段说明.md`；新增需求必须优先复用已确认名称，不得重新引入同义别名。
- 等级字段统一为【VIP等级】；【会员等级】只允许作为菜单或页面名称使用。
- 筛选统一使用【所属站点】；数据表默认使用【所属站点】，仅当页面业务主体本身就是站点时使用【站点名称】。
- 普通资金订单统一使用【单号】，投注业务使用【投注单号】，返水业务使用【返水订单号】。
- 提款业务统一使用“提款”，禁止再混用“取款”或“提现”；金额字段统一为【提款金额】，到账字段统一为【实际到账】。
- 表格中的上级代理必须拆为【上级代理账号】和【上级代理编号】两列；筛选仍使用【上级代理账号/编号】智能搜索框。
- VIP福利统一使用【VIP晋升礼金、VIP生日礼金、VIP周礼金、VIP月礼金】；晋升门槛使用【晋升存款、晋升流水】。
- 会员视角汇总字段统一为【总输赢（会员视角）】，单笔字段统一为【输赢金额（会员视角）】。
- 配置维护元数据统一使用【最后操作人、最后操作时间】；会员关联数量统一使用【会员数量】。
- 全项目跨业务识别会员的主要字段统一为【会员账号】。后台确实存在【会员ID】，但只在会员管理、会员详情等明确需要后台辅助识别的位置展示；其他业务页面默认不展示，也不得用会员ID替代会员账号。
- 所有需求的筛选区中，【所属站点】统一使用可多选的下拉组件；默认全选全部站点，支持逐项勾选、取消和一键全选。禁止退回单选下拉或自由文本输入。动态Tab、弹窗和后续新增页面同样执行。
- 生产站点固定枚举及简称：星跃体育（XY体育）、拉布布（拉布布）、旺财体育（WC体育）、财神体育（CS体育）、亚云体育（YY体育）、尼萨体育（NS体育）、DW体育（DW体育）。所有站点筛选及需要完整列出站点的控件必须使用简称，并完整展示【XY体育、拉布布、WC体育、CS体育、YY体育、NS体育、DW体育】；禁止继续使用“新旺体育、彩虹站、财源客栈、站点A”等虚构选项。
- 生产场馆类型固定枚举：【电子、真人、体育、棋牌、彩票、捕鱼、电竞】。
- 生产全场馆固定枚举：【Choice真人、BBIN真人、DB捕鱼、DB彩票、DB电子、DB多宝电子、沙巴体育、DG视讯、DB电竞、开元棋牌、Sex真人、EVOPLAY电子、FC电子、DB哈希、FB体育、JDB电子、IM体育、MG电子、NetEnt电子、PG电子、PA捕鱼、VR彩票、IM电竞、KingsMidas棋牌、PP电子、PP真人、RED TIGER电子、SBO体育、V8棋牌、CMD体育、EVOLUTION真人、天成彩票、雷火电竞、旺财棋牌、DB真人、CQ9电子、熊猫体育、DB棋牌、GOD Lottery】。涉及场馆选择、配置或完整场馆展示时必须全部列出，不得用少量示例替代。
- 红利类型筛选、场馆名称筛选及其他明确要求完整枚举的控件必须列出全部有效选项，不得只放示例值。红利类型统一复用项目公共红利枚举；场馆名称统一读取 `venue-games.js` 与 `docs/场馆游戏对应表.xlsx`，禁止页面内手写不完整子集。
- 场馆与游戏（厅）的对应关系以本地 `docs/场馆游戏对应表.xlsx` 为唯一事实来源；原型要求完整展示时不得截断或省略。固定场馆名单中若 Excel 暂无对应游戏，仍保留场馆入口并展示明确空状态，不得虚构游戏。
- 代理后台是单站点后台，代理只能查看当前登录账号所属站点的数据，不存在跨站筛选；代理后台所有页面禁止展示【所属站点】【站点】【站点名称】筛选。该例外高于上一条全局站点筛选规则，但不影响表格、详情或个人信息中按业务需要展示站点归属。
- 所有需求的筛选区中，【代理账号】和【代理编号】统一合并为一个【代理账号/编号】智能搜索框；输入账号或编号任一部分均实时匹配候选代理，候选项同时展示账号与编号，选择后保留两者映射。上级代理筛选使用同一规则并命名为【上级代理账号/编号】。数据表展示列不因此强制合并。
- 文本输入默认在失焦、查询或提交前 trim，只去首尾空格；说明必须列出受影响的具体字段名。
- 联想输入仅在获得焦点后展示；无输入显示全部，输入后过滤，选择后回填，失焦收起。
- 筛选按钮排第一，点击面积约为重置、导出等次级按钮的两倍；筛选组件说明统一写明该差异。
- 会员 VIP 等级没有特殊规则时，说明只写“读取VIP等级列表即可”，不固化数量。
- 地区筛选统一使用国家、省、市联动；是否允许“全部”按具体需求说明。
- 所有类型、状态、来源、方式、结果、等级、分类等枚举字段，统一遵循 `docs/统一枚举说明.md`；必须在需求或右侧说明中列出完整值域，跨页面复用同一业务枚举时只允许引用统一枚举，不得各写一套近似值。
- 动态枚举不得伪造固定选项，必须明确数据来源、筛选范围、排序规则及停用项是否可见；禁止只写“读取平台枚举”“全部类型”或“沿用现状”而不说明具体来源。
- 状态枚举除展示值外，还必须说明关键状态映射和转换边界；自由填写的原因、备注、标题等文本字段不得误列为固定枚举。
- 可见控件必须可操作；未实现或待未来接口的动作要明确标注，不能假装已完成。

## 日期时间规则

- 数据筛选的时间范围默认使用一个组件和一个双日历弹层，同时选择开始和结束。
- 默认精确到秒，格式 `YYYY-MM-DD HH:mm:ss`；开始日期默认 `00:00:00`，结束默认当前日期和当前时刻。
- 默认快捷项：今日、昨日、本周、30 天、90 天、180 天。
- 开始和结束边界均包含，开始不得晚于结束；具体页面仍需说明默认值、最大跨度、清空、手输、非法值和时区。
- 点击开始或结束区域都打开同一个弹层；确认和取消后立即关闭。
- 弹层打开时根据业务内容区左右边界动态定位，不得被左侧菜单或右侧说明区截断。

## 数据表与分页规则

- 数据表中的【下注详情】统一使用同一套交互：单元格默认展示前 3 行，鼠标悬停或键盘聚焦后以只读气泡展示全部字段；电子、棋牌、捕鱼在接口无详情时只显示游戏名称；导出时全部详情放在同一单元格并按字段换行。下注详情气泡必须高于表格固定表头、固定列和正文，且不得被表格滚动容器裁切。后续需求不得另做点击弹窗或近似交互。
- 所有数据表自身必须设置 `width: 100%` 并至少充满所属容器，包括 Tab、弹窗、抽屉和动态切换后生成的表格；字段较少时随容器拉伸，字段较多时叠加合理 `min-width`，由表格容器承担横向滚动。禁止只依赖 `width: max-content`，也禁止表格右侧出现无意义留白。
- 数据表正文、汇总行和单元格提示中不重复显示 `CNY`；统一把币种写在对应金额列表头，例如【订单金额（CNY）】。非表格的概览卡、表单单位和业务文案不受此规则影响；多币种表格必须保留独立币种列或在表头明确币种口径。
- 数据表中【输赢】【盈亏】等结果只用正数、负数和 `0` 表达，不在数值前重复显示“输”“赢”“全输”“全赢”等文字；半赢、半输等玩法细节只放在下注详情等业务明细中。
- 数据表的右侧说明不得重复罗列表头或字段清单；开发可直接从原型读取的列名不写说明，只记录无法从界面判断的数据口径、计算规则、固定列、排序、分页、状态、权限和异常边界。
- 每个数据表都必须在对应右侧说明中单独写明默认每页条数；弹窗内数据表也要单独说明，不得只在公共规则或其他表格说明中笼统描述。
- 每个支持分页的数据表说明都必须单独包含固定文案【支持最多200条/页的选项】；页面表格、Tab表格和弹窗内表格均不得遗漏，固定单行概览、固定矩阵及明确不分页的展示表除外。
- 默认分页数量为 `20`，选项固定为 `10 / 20 / 50 / 100 / 200`，并提供总数、上一页、连续页码、当前页、下一页和跳页。
- 默认每页 `50` 的表格在标题栏右侧和表格底部各放一套分页器；其他分页数量不增加顶部分页器。
- 所有原型数据表最多直接显示 10 行，超过后表格内部出现纵向滚动；该规则只实现，不写入右侧说明。
- 所有滚动区域的横向滚动条高度和纵向滚动条宽度均为 `15px`，轨道与滑块常态可辨识；无溢出时隐藏。
- 数据表说明若需要滚动条规则，只能使用一条完整文案：“表格仅在内容超出对应可视区域时显示滚动条：横向滚动条高度、纵向滚动条宽度均保持15px；内容可完整显示时对应滚动条隐藏”。不得拆句或只写一个方向。
- 删除“数据表高度随数据条数自适应，不设置内部纵向滚动条”等冲突说明。
- 宽表格由表格容器承担横向滚动，不能让整个页面或右侧说明区跟随横移。
- 固定列的 `left/right`、宽度和阴影必须成组定义；横向滚到最右侧时，最后一列不得被右侧说明区遮挡。
- 金额有小数时按业务精度显示，无小数时显示整数，不强制 `.00`；金额略大并加粗。
- “上级代理”是统一表头；复合单元格内使用“代理用户名”“代理编号”。不同语义字段分行并带标题。
- 数据表需要上下两处总计时，上方总计必须放在表头外、位于表头上方；上下总计均与对应数据列严格对齐，并与数据表共享横向滚动。

## 导出规则

- 所有包含【导出】或【导出表格】的页面和弹窗，右侧必须展示统一的醒目导出说明，不得只写“导出当前结果”。
- 页面自身的导出规则与全局 Excel 导出规范必须合并为同一个右侧说明，并与左侧【导出】或【导出表格】按钮使用同一说明 ID 双向关联；禁止拆成两个重复说明。
- 导出文件的字段、顺序、字段含义和金额币种必须与当前后台数据表逐列对应，不得遗漏、合并或省略后台已展示字段；需求另有明确导出扩展字段时，在此基础上追加。
- 导出结果不得产生无业务含义的空白行；分页、分组和多行详情不得额外插入空行。
- 数量、金额、比例等可计算数字必须以 Excel 数值类型写入对应单元格，禁止导出为带单位、千分位文本或其他无法直接求和的字符串；单位放在表头，日期时间保持可识别的日期时间格式。

## 弹窗与层级规则

统一层级阶梯，新增浮层不得随意写更大的孤立数值：

- `0-4`：普通页面内容。
- `5-8`：表格固定列和固定表头。
- `12-20`：联想下拉、局部浮层、带角标表格单元格。
- `40`：页面内确认遮罩。
- `70-80`：已打开的日期时间字段、日期弹层和页面内提示气泡。
- `90`：组件说明角标，必须高于固定表头、固定列及页面内局部浮层，但低于模态弹窗。
- `100`：模态遮罩与弹窗，必须高于页面内所有角标、日期弹层和固定列。
- `110+`：仅允许弹窗内部的二级下拉、日期弹层或提示使用。

弹窗尺寸按内容类型统一：

- 普通确认或表单弹窗：约 `520px`。
- 关联列表弹窗：约 `720-760px`。
- 多列表格弹窗：可到 `1120px`，但始终 `max-width: 100%`。
- 弹窗内表格设置合理 `min-width`，超宽时只在 `.modal-body` 或表格容器内横向滚动，不撑宽弹窗、不造成页面级横向滚动。
- 弹窗表格同样执行最多 10 行和 `15px` 滚动条规则；内容不足时不显示无意义滚动条。
- 桌面端模态遮罩只覆盖左侧原型区，不能挡住右侧说明；窄屏可覆盖全屏。
- 危险动作必须二次确认，危险动词和后果醒目；取消、关闭不得产生副作用。
- 关闭弹窗时清理其 DOM 和临时状态，避免重复绑定事件。

## 视觉基线

- 视觉已由用户确认，除非用户明确要求整体改版，否则禁止修改主题、色系、圆角、阴影和排版语言。
- `styles.css` 是唯一视觉事实来源；新增页面前先读 `:root` 和相近组件样式，优先复用变量和类。
- 目标是克制、专业、低饱和、低品牌识别度，避免提前误导 UI；不用高饱和色、大面积渐变、光晕、玻璃拟态、大圆角、卡片套卡片或营销式布局。
- 核心 Token：`--ink #17201b`、`--muted #66716b`、`--line #dce2de`、`--paper #fff`、`--canvas #eef1ef`、`--green #186b4d`、`--green-soft #e7f1ec`、`--amber #a85f13`、`--red #b6382d`、`--blue #225fad`。
- 深色导航继续使用 `#18231d / #1c2721 / #26362e` 附近的灰绿，不改成纯黑、深蓝或紫色。
- 业务区正文、表单、导航和表格默认 `13px`，不得低于 `12px`；辅助信息不得低于 `11px`；右侧说明正文默认 `13px`。
- 主按钮深绿，次按钮白/浅灰底细边框，禁用按钮灰色；禁用态不得影响红底白字角标。
- 所有原型组件角标文字强制使用 `#FFFFFF`；按钮禁用态、父级透明度和局部选择器不得把角标文字改成灰色或降低其可读性。
- 标签使用低饱和语义色，同时保留文字；颜色不能是唯一状态信号。
- 一级、二级菜单保持同级可读性；不因层级把一级菜单做得更小。
- 局部视觉修复只处理该问题及同类可复现问题，不借机重做全局视觉。

## 编码与数据

- 优先使用项目现有技术栈和公共函数；没有明确收益不引入框架、依赖或多层抽象。
- 需求、页面、组件和说明通过稳定 ID 关联，不依赖数组位置或显示文案。
- 渲染与需求数据分离；公共规则只维护一份，禁止散落硬编码。
- 用户可见文案应集中或可检索；改名或删除时同步检查导航、说明、深链接和历史引用。
- 示例数据必须脱敏，并覆盖当前需求需要的正常、空、边界或异常状态；不得连接生产接口或产生真实副作用。
- 动态内容必须转义；不得写入密码、Token、私钥、真实用户数据等敏感信息。
- 修改共享逻辑时检查所有使用页面，不做与需求无关的顺手重构。
- 静态资源变更后更新 `index.html` 的查询版本，避免浏览器继续使用旧缓存。

## 每次需求结束复盘

- 对照本次实现列出新增的通用交互、样式、数据结构和踩坑经验。
- 只把可跨需求复用的结论写入本文件；字段含义、特定页面逻辑、当次待确认项继续留在对应需求文档或 `data.js`，不得污染长期规范。
- 新能力已出现两次或预计很快复用时，加入“公共组件目录”并优先抽取；只出现一次且业务专用时暂不抽象。
- 检查新规则是否与旧规则冲突，冲突时保留用户最新确认项并删除旧规则，禁止同时保留两套说法。
- 复盘后保持文档精简：同一规则只写一次，能由代码和 `styles.css` 明确得出的实现细节不重复抄写。

## 验收清单

- 需求列表、详情路由、页面导航、刷新和直接深链接可用。
- 原型与说明一致；组件 ID 唯一，左右定位、高亮和键盘激活正确。
- 关键按钮、开关、Tab、筛选、日期、弹窗、批量操作和未来接口入口可按说明演示。
- 空、加载、错误、禁用、成功等适用状态有明确表现。
- 表格十行限制、横纵滚动、固定列、分页和弹窗尺寸正确，右侧说明区不遮挡内容。
- 日期弹层、角标、固定列、遮罩和模态弹窗层级无穿透。
- 常见桌面与窄屏下无文字重叠、内容截断或不可达操作。
- `node --check app.js`、`node --check data.js`、`git diff --check` 通过；浏览器控制台无影响使用的错误。
- 完成后说明已修改内容、验证结果、未完成项；未运行的检查必须明确告知。

## GitHub 发布

- 默认远程仓库：`https://github.com/Mike-oss-966/prototypes.git`，默认分支：`main`。
- 本机浏览器通过 v2rayN 访问外网，Git 直连 `github.com:443` 会超时或被重置；GitHub 的 `fetch`、`pull`、`push` 统一使用单次代理参数 `git -c http.proxy=http://127.0.0.1:10808 <命令>`。推送前先用同一参数执行 `fetch origin main`，禁止在已知直连不可用时反复尝试直连。该参数只对当前命令生效，不写入全局 Git 配置。
- 仅当用户明确说“推到 GitHub”“上传 GitHub”等时，才检查、提交并推送；普通修改不得自动发布。
- 推送前检查工作区、`docs/` 忽略状态、语法、差异和远程最新提交；不得强推或覆盖未知提交。
- 推送前必须检查 `docs/GitHub推送待确认事项.md`；其中未确认的已完成需求改动必须先取得用户对具体清单的明确上传确认。
- 使用本机 Git Credential Manager；绝不要求用户在对话中提供密码、PAT、验证码、SSH 私钥或恢复代码。
- 发布成功后报告提交号和分支；失败时说明具体阶段，不反复盲目重试。
